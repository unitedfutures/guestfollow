-- ============================================================
-- 宿泊価格：施設ごとの自動プライシングのルールを保存
--
--   pricing_rules : {
--     "weekday": 12000,      -- 平日価格
--     "saturday": 18000,     -- 土曜価格
--     "preHoliday": 18000,   -- 祝前日価格
--     "floor": 10000,        -- 最低価格（下限）
--     "minStayDefault": 1,   -- 最低宿泊日数（既定）
--     "minStayByDow": { "0":1,"1":1,"2":1,"3":1,"4":1,"5":2,"6":2 }  -- 曜日別（0=日）
--   }
-- ============================================================
ALTER TABLE public.facilities
  ADD COLUMN IF NOT EXISTS pricing_rules jsonb;
